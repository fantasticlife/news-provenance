<div class="section">
	<%= content_tag( 'h1', "Similar sources to #{@source.title}" ) %>
	<table>
		<thead>
			<tr>
				<%= content_tag( 'td', 'Similarity' ) %>
				<%= content_tag( 'td', 'Source' ) %>
			</tr>
		</thead>
		<tbody>
			<%= render :partial => 'similar_source', :collection => @source.source_similarities %>
		</tbody>
	</table>
</div>